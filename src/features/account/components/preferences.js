import { languageSelector } from '../../../components/language-selector/language-selector.js';
import { currencySelector } from '../../../components/currency-selector/currency-selector.js';
export function preferencesView(preferences) {
  return `<form class="account-panel" data-form="preferences"><h2>A tu manera</h2><p class="account-muted">Guarda cómo prefieres encontrarte con nuestros productos.</p><div class="account-form-grid">${languageSelector({ id: 'account-language', value: preferences.language, disabled: false })}${currencySelector({ id: 'account-currency', value: preferences.currency, disabled: false })}</div><div class="account-preferences-checks">${[['rememberCurrency', 'Recordar moneda seleccionada'], ['showFeatured', 'Mostrar productos destacados'], ['receiveNews', 'Recibir novedades']].map(([key, label]) => `<label class="account-check"><input type="checkbox" name="${key}" ${preferences[key] ? 'checked' : ''}> ${label}</label>`).join('')}</div><p class="account-muted">Estas preferencias se guardan en tu cuenta de demostración. El catálogo mantiene su presentación actual y no se enviarán novedades.</p><div class="account-actions"><button class="button" type="submit">Guardar preferencias</button><button class="button button-secondary" type="button" data-action="cancel-preferences">Cancelar</button></div></form>`;
}
