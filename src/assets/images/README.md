# Imágenes locales

Guardar aquí las fotografías aprobadas por cada grupo. `image: null` indica que todavía no hay imagen. Las tarjetas actuales muestran un placeholder HTML/CSS; la carga de imágenes se incorporará después.
